import { Grid, Text } from "@chakra-ui/react";

export const Error = () => {
  return (
    <Grid placeItems="center" height="100vh">
      <Text fontSize="5xl">Sorry but an error show up when loading data</Text>
    </Grid>
  );
};
